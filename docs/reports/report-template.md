# Experiment Report Template

每份 summary 至少包含：实验目的、commit、JDK/OS、profile、参数与样本量、raw 文件链接、关键统计/成功率、异常样本、阈值判断以及失败后的整改记录。

机器可读原始数据必须进入 `docs/reports/raw/`；分析性 Markdown 必须进入 `docs/reports/summary/`。历史 `docs/reports/*.md` 保留为升级前或早期迭代的基线资料。
